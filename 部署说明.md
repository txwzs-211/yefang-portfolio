# 作品集网站 · 部署与维护说明

## 网站地址

- **线上地址**：https://yefang-portfolio.vercel.app （部署后确认实际域名）
- **代码仓库**：https://github.com/txwzs-211/yefang-portfolio

## 日常维护：怎么改内容

改完代码后，只要推送，Vercel 会自动重新部署（约 1 分钟）：

```bash
cd "E:/桌面缓存/Personal project/portfolio website-小叶"
git add -A
git commit -m "更新说明"
git push
```

## 最常改的地方

| 想改什么 | 改哪个文件 |
|---|---|
| 作品列表、标题、简介 | `src/data/works.js` |
| 技术栈板块的项目详情 | `src/data/projects.js` |
| 个人简介、照片 | `src/components/About.jsx` |
| 联系方式 | `src/components/Contact.jsx` |
| 页面配色、字体、动画 | `src/index.css` |

## 本地预览（改完先看效果再推）

```bash
cd "E:/桌面缓存/Personal project/portfolio website-小叶"
npm run dev
```

浏览器打开 http://localhost:5173 ，改代码会自动刷新。

## 素材替换规范

**视频**：放进 `public/assets/`，单文件控制在 **20MB 以内**。
超过的话用压缩脚本：

```bash
bash scripts/compress-for-web.sh
```

**图片**：放进 `public/assets/`，推荐用 **WebP** 格式（同样画质下体积只有 PNG 的 5%）。

转格式：

```bash
bash scripts/compress-images.sh
```

新的 PNG 转 WebP 后，记得把源码里的 `.png` 改成 `.webp`：

```bash
python scripts/switch-to-webp.py
```

## 上线前自检

```bash
python scripts/check-assets.py
```

会检查所有引用的图片视频是否都存在，避免上线后出现 404 空白。

## 重要：GitHub 单文件限制

GitHub **不允许超过 100MB 的单个文件**。任何素材超过 100MB 都推不上去。
务必先用压缩脚本处理。

## 原始素材备份

压缩前的原始文件保存在（已被 git 忽略，不会上传）：

- `_original_videos/` — 原始视频
- `_original_images/` — 原始图片

**这两个文件夹不要删**，需要重新压制时用得上。它们不占仓库空间。

## 技术栈

- React 19 + Vite 8
- 部署：Vercel（连接 GitHub 仓库自动部署）
- 构建产物：`dist/`
